# Docker Compose secrets only (interpolated into containers as APP_KEY / DB_PASSWORD).
# Local development uses \`composer dev\` + a normal Laravel .env (see .env.example).
#
# Names are LudoTest-specific so they never collide with Immich's DB_* vars.

LUDOTEST_APP_KEY=
LUDOTEST_DB_PASSWORD=
